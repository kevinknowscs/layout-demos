import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'

// Uncomment the next line if you want some start root CSS styles
import '../index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
